{{!< default}}

<header id="blog-header"{{#if tag.image}} class="has-cover"{{else}}{{#if @blog.cover}} class="has-cover"{{/if}}{{/if}}>
	<div class="inner">
		<nav id="navigation">
			{{#if @blog.logo}}
			<span class="blog-logo">
				<a href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="Blog Logo" /></a>
			</span>
			{{else}}
			<span id="home-button" class="nav-button">
				<a class="home-button" href="{{@blog.url}}" title="Home"><i class="ic ic-arrow-left"></i> Home</a>
			</span>
			{{/if}}
			{{#if @blog.navigation}}
			<span id="menu-button" class="nav-button">
				<a class="menu-button github-button" target="_blank" href="http://github.com/chrisgillis/"><i class="fa fa-lg fa-github-alt"></i> Github</a>
				<a class="menu-button" target="_blank" href="{{@blog.url}}/rss/"><i class="ic ic-rss"></i> Subscribe</a>
			{{/if}}
		</nav>
		<h1 class="blog-name">{{tag.name}}</h1>
		<span class="blog-description">
		{{#if tag.description}}
			{{tag.description}}
		{{else}}
			Posts: {{pagination.total}}
		{{/if}}
		</span>
		{{#if tag.image}}
		<div class="blog-cover cover" style="background-image: url('{{tag.image}}');"></div>
		{{else}}
		{{#if @blog.cover}}
		<div class="blog-cover cover" style="background-image: url('{{@blog.cover}}');"></div>
		{{/if}}
		{{/if}}
	</div>
</header>

<div id="index" class="container">

	<main class="content" role="main">
	
		{{> "loop"}}
	
	</main>

</div>